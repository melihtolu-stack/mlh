# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Backend API URL (required for email send + incoming webhook)
# Production'da backend'in canlı URL'ini kullanın (örn: https://backend-mlh.heni.com.tr)
# Backend'i Coolify'da ayrı bir servis olarak deploy edin (BACKEND_DEPLOYMENT.md'ye bakın)
BACKEND_URL=http://localhost:8000

# E-posta: info@heni.com.tr
# Gelen mailler bu adrese yönlendirilir, webhook /api/emails/incoming'e POST edilir.
EMAIL_INBOX=info@heni.com.tr

# Backend .env (backend klasöründe) – SMTP + gönderen adres
# SMTP_SERVER=smtp.hostinger.com
# SMTP_PORT=465
# SMTP_USERNAME=info@heni.com.tr
# SMTP_PASSWORD=...
# FROM_EMAIL=info@heni.com.tr
# FROM_NAME=Heni

# Node Environment
NODE_ENV=production
